# Working on Token Trail

Run the collector and fixture tests; do not read personal logs into model context to reproduce the analysis. Use synthetic fixtures for development, screenshots and bug reports. Never commit `data/`, local databases, personal diagnoses, private paths, chat titles or authentication material. Public screenshots must use `demo`.

Preserve the accounting contract: exclusive fresh/cache-read/cache-write input, reasoning as an output subset, stable response deduplication and explicit lineage evidence. Add focused regression fixtures when changing an adapter. Missing provider fields are unknown, not estimated billing. Do not claim a usage category proves waste.

Keep runtime dependencies at zero where practical. New providers should adapt the existing dashboard through data, not duplicate its UI. Execute bundled code and read only relevant implementation sections when changing them. Keep skill entry points concise; load provider references only when needed.
